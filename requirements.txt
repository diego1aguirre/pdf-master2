flask>=3.0.0
pypdf>=4.0.0
reportlab>=4.0.0
python-docx>=1.1.0
mammoth>=1.6.0
# weasyprint requires system pango lib — works locally, skipped on Vercel (no system libs)
# weasyprint>=60.0
# streamlit is only for the local alternative UI, not needed for the web app
# streamlit>=1.28.0
# docx2pdf requires Microsoft Word — only useful on Windows/Mac with Word installed
# docx2pdf>=0.1.8
